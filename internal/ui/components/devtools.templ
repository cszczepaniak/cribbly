package components

import "github.com/cszczepaniak/cribbly/internal/server/middleware"

templ DevTools() {
	if (!middleware.IsProd(ctx) || middleware.DevToolsFromQuery(ctx)) && middleware.IsAdmin(ctx) {
		{ children... }
	}
}
